#include "Globals.h"

// Define the global calibration variables.
// Initialize offsets to 0 and scale factors to 1.
float offsetX = 0;
float offsetY = 0;
float offsetZ = 0;
float scaleX = 1;
float scaleY = 1;
float scaleZ = 1;
